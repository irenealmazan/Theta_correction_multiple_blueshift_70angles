function [f0]= formfact(zed, k)% FORMFACT switchyard routine for deciding whether to use%          Cromer & Mann for the atomic form factor (sfcoef)%          or Waasmeier & Kirfel (sfwaas)% Usage: [f0]= form_fact(zed, k)% % Input:%           k =  array of 4*pi*sin(theta)/lambda (inv. Angstroms)% 		    z =  atomic number% Output:%           f0 = column vector of atomic form factors% 		  % If FF_FLAG== 0, sfwaas is used, FF_FLAG==1, sfcoef is used% % 5-May-03 SMB bren@slac.stanford.eduglobal FF_FLAGif FF_FLAG==0,	f0 = sfwaas(zed,k);else	f0 =  sfcoef(zed,k);endf0= f0(:);	% make it a column vector